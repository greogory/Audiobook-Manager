# Audiobooks Configuration
# This file contains paths and settings for the audiobook library.
# Edit these values to match your system configuration.

# =============================================================================
# REQUIRED PATHS
# =============================================================================

# Path to your audiobook collection
# This is where your .m4b, .opus, .mp3 files are stored
AUDIOBOOK_DIR="/raid0/Audiobooks"

# Path where this project is installed
# Usually auto-detected, but can be overridden
PROJECT_DIR="/raid0/ClaudeCodeProjects/Audiobooks"

# =============================================================================
# OPTIONAL PATHS (derived from above, override if needed)
# =============================================================================

# Path to the database file
DATABASE_PATH="${PROJECT_DIR}/library/backend/audiobooks.db"

# Path to cover art storage
COVER_DIR="${PROJECT_DIR}/library/web/covers"

# Path to JSON data output
DATA_DIR="${PROJECT_DIR}/library/data"

# =============================================================================
# SERVER PORTS
# =============================================================================

# Web interface port (default: 8090)
WEB_PORT=8090

# API server port (default: 5001)
API_PORT=5001

# =============================================================================
# OPUS CONVERSION PATHS (for AAXtoMP3 converter)
# =============================================================================

# Directory for converted OPUS files
OPUS_DIR="${AUDIOBOOK_DIR}/Audiobooks-Converted-Opus-nocomp"

# Directory for converted M4B files
CONVERTED_DIR="${AUDIOBOOK_DIR}/converted"
